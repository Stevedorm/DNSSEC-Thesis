# DNSSEC-Thesis (LaTeX)

This repository contains my thesis manuscript written in LaTeX.

The pdf form of the write-up is in the _build directory

## Build (local)
Requirements:
- TeX Live (recommended) or MiKTeX
- latexmk

Clean:
```bash
./scripts/clean.sh
```
Build:
```bash
./scripts/build.sh
```
